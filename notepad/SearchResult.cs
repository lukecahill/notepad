namespace notepad {
	/// <summary>
	/// SearchResults which contains the position and length of the search results. Used to add to a list to cycle through.
	/// </summary>
	public class SearchResult {
		public int Postition { get; set; }
		public int Length { get; set; }
	}
}
